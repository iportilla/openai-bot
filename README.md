# openai-bot

1. Clone repo
   
    `git clone repo`
   
2. Copy env.sample to .env
   
   `cp env.sample .env`
   
3. Install dependencies
 
   `pip install -r requirements.txt
   
4. run ST UI
 
   `streamlit run app.py`
